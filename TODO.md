# Meriams App - TODO

## Setup & run-ready
- [x] Add `backend/.env.example` with required placeholders
- [x] Add/update README section for exact MySQL setup + seed + admin login
- [ ] Run verification: `npm run install-all` then `npm start`
- [ ] Verify endpoints: `/api/auth/*`, `/api/products`, `/api/orders/user`, `/api/contact`
- [ ] Verify admin access: login as admin and load `/admin.html`


